NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Needed only for the admin "create account" screen. Never expose this in the browser.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Optional: enables CV assessment; the UI uses a deterministic fallback without it.
OPENAI_API_KEY=
